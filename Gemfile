source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in evented.gemspec
group :development do
  gemspec
  gem 'guard-rspec', require: false
end
